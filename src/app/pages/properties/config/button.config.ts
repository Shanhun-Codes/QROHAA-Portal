import { ButtonConfig } from '../../../shared/components/button/button.config';

export const PROPERTY_BUTTON_CONFIG: ButtonConfig = {
  label: 'Create Property',
  icon: 'add',
  variant: 'primary',
  size: 'md',
};

export const ARCHIVE_PROPERTY_BUTTON_CONFIG: ButtonConfig = {
  label: 'Archive',
  icon: 'archive',
  variant: 'primary',
  size: 'sm',
};

export const RESTORE_PROPERTY_BUTTON_CONFIG: ButtonConfig = {
  label: 'Restore',
  icon: 'unarchive',
  variant: 'primary',
  size: 'sm',
};
