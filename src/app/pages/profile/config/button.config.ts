import { ButtonConfig } from '../../../shared/components/button/button.config';

export const EDIT_PROFILE_BUTTON_CONFIG: ButtonConfig = {
  label: 'Update',
  variant: 'primary',
};

export const EDIT_BRANDING_BUTTON_CONFIG: ButtonConfig = {
  label: 'Change',
  variant: 'primary',
};

export const EDIT_QUESTIONS_BUTTON_CONFIG: ButtonConfig = {
  label: 'Choose New',
  variant: 'primary',
};

export const PREVIEW_BUTTON_CONFIG: ButtonConfig = {
  label: 'Preview Public Branding',
  variant: 'danger',
};
